name: Web - Accessibility Testing and Deployment

on:
  pull_request:
    branches: [ staging, main ]
    paths:
      - 'web/**'
      - 'bin/**'
      - '.github/workflows/test-and-deploy-reusable.yml'

permissions:
  id-token: write
  contents: read

jobs:
  test-and-deploy:
    uses: ./.github/workflows/test-and-deploy-reusable.yml
    with:
      application: 'web'
      deploy-path: '/var/www/web.witchcraft-and-wizardry.school'
      test-mode: ${{ github.base_ref == 'main' && 'full' || 'abbreviated' }}
      ignores: 'lessons'
    secrets:
      WAVE_API_KEY: ${{ secrets.WAVE_API_KEY }}
      NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
      SERVER_USERNAME: ${{ secrets.SERVER_USERNAME }}
      SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}