name: Nightly Full Tests - API

on:
  schedule:
    # Run at 02:00 UTC daily
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  id-token: write
  contents: read

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      has-changes: ${{ steps.check.outputs.has-changes }}
    steps:
      - name: Checkout staging branch
        uses: actions/checkout@v6
        with:
          ref: staging
          fetch-depth: 0

      - name: Check for changes in last 24 hours
        id: check
        run: |
          # Check if there are any commits in the last 24 hours on staging
          CHANGES=$(git log --since='24 hours ago' --oneline -- api/ bin/ .github/workflows/ | wc -l)
          if [ "$CHANGES" -gt 0 ]; then
            echo "has-changes=true" >> "$GITHUB_OUTPUT"
            echo "Found $CHANGES commit(s) in the last 24 hours"
          else
            echo "has-changes=false" >> "$GITHUB_OUTPUT"
            echo "No changes in the last 24 hours"
          fi

  run-full-tests:
    needs: check-changes
    if: needs.check-changes.outputs.has-changes == 'true'
    uses: ./.github/workflows/test-and-deploy-reusable.yml
    with:
      application: 'api'
      deploy-path: '/var/www/api.witchcraft-and-wizardry.school'
      test-mode: 'full'
      use-azure-keyvault: true
      # Replace with your Azure Key Vault and federated identity values.
      keyvault-name: magic-kv
      azure-client-id: b96a868a-23a8-4b5e-8f78-274dd57d6a9b
      azure-tenant-id: 9a2a3100-fefb-41f9-8640-d4f22f3868e1
      azure-subscription-id: 7eac63bd-20a8-4559-9411-8bb42f97bc6c
    secrets:
      WAVE_API_KEY: ${{ secrets.WAVE_API_KEY }}
      NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
